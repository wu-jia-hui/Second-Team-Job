/* app.wxss */
.container {
  padding: 20px;
}

.header {
  font-size: 24px;
  font-weight: bold;
  margin-bottom: 20px;
}

.picker {
  margin-bottom: 20px;
  padding: 10px;
  background-color: #f8f8f8;
  border-radius: 5px;
  text-align: center;
}

.calendar {
  margin-bottom: 20px;
}

.footer {
  display: flex;
  justify-content: space-around;
  margin-bottom: 20px;
}

.sign-button, .add-button {
  width: 100px;
  height: 40px;
  border-radius: 20px;
  border: none;
  color: white;
  font-size: 16px;
}

.sign-button {
  background-color: #007aff;
}

.add-button {
  background-color: #ff3b30;
}

/* app.wxss */
.container {
    padding: 20px;
}

/* 其他样式... */

.sign-button, .add-button {
    width: 80px; /* 按钮宽度 */
    height: 80px; /* 按钮高度 */
    line-height: 80px; /* 文字垂直居中 */
    border-radius: 50%; /* 圆形按钮 */
    border: none;
    color: white;
    font-size: 16px;
    text-align: center; /* 文字居中 */
    margin: 10px; /* 按钮之间的间距 */
}

.sign-button {
    background-color: #007aff;
}

.add-button {
    background-color: #ff3b30;
}

/* app.wxss */
.container {
  padding: 20px;
}

/* 圆形按钮样式 */
.circle-button {
  width: 80px; /* 按钮宽度 */
  height: 80px; /* 按钮高度 */
  border-radius: 50%; /* 圆形 */
  border: none; /* 无边框 */
  background-color: #007aff; /* 按钮背景色 */
  color: white; /* 文字颜色 */
  font-size: 16px; /* 字体大小 */
  display: flex; /* Flex布局 */
  justify-content: center; /* 水平居中 */
  align-items: center; /* 垂直居中 */
  margin-top: 10px; /* 顶部外边距 */
}

/* 已签到按钮样式 */
.sign-button {
  background-color: #ff3b30; /* 已签到按钮背景色 */
}